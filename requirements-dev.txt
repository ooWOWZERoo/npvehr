# Test-only dependencies. Not installed on Vercel (which builds from
# pyproject.toml's [project.dependencies]) -- these are for local/CI use:
#   pip install -r requirements.txt -r requirements-dev.txt
#   playwright install chromium   # first time only, downloads the browser
#   pytest
-r requirements.txt
pytest>=8.0.0
pytest-playwright>=0.5.0
